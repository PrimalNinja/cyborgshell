# Cyborg Shell: The Complete Stack

## ZOSCII
- Information-theoretic security (I(M;A)=0)
- Quantum-proof by mathematics, not computation
- Works with public ROMs
- Plausible deniability built-in
- No key deletion ritual needed (ZOSCII encoding coming)
- Safe on public computers
- No vendor dependency

## CyborgShell (Open Source)
- Browser-first execution (code runs client-side)
- Server = file storage only (no code execution)
- API keys never leave browser local storage
- Direct AI calls (browser → Claude/OpenAI/Gemini/Ollama)
- AI-Agent-First (AIs use identical interface to humans)
- AIs can use it like humans do (Claude proved it)
- Applications use LLMs as native functions
- Full webapp development environment
- No user tracking
- No IP tracking
- Zero platform fees (MIT licensed)
- Air-gap capable (works offline with Ollama)
- Self-host on any PHP server
- Runs on OLPC from 2014
- Works on Xbox One
- Mobile-first (phone, tablet, any browser)
- Zero login required (public instance)
- No registration needed
- Immediate access (no friction)
- Sessions maintain context
- Session training (load context from files/other sessions)
- Sessions can be saved (persist knowledge bases)
- Sessions can be loaded (reuse trained context)
- Sessions can be merged (combine different LLM sessions)
- Sessions reusable with different LLMs (same training, different provider)
- Knowledge base building (train sessions incrementally)
- RAG (Retrieval-Augmented Generation) native (sessions = knowledge retrieval)
- RAG without separate vector database (file-based retrieval)
- Service switching (Claude → OpenAI → Gemini)
- Multi-AI flexibility (use multiple providers simultaneously)
- Orchestration projects (save DAG workflows as reusable projects)
- Project save/load (persist complex workflows)
- Project merge (combine orchestration pipelines)
- Orchestration without external tools (native in CyborgShell)
- Multi-stage pipelines (orchestrate complex workflows)
- Transformer chaining (link multiple processors)
- Conditional routing (quality gates, branching logic)
- Parallel execution (multi-input, non-blocking)
- Batch processing (apply orchestration to many files)
- DAG orchestration (files as nodes, links as edges)
- Auto-cascading on dirty flag
- Projects as version-controllable JSON
- Multi-input file operations (fan-in, fan-out, merge)
- Quality gates native
- Template system
- MIME type support (text and binary files)
- Files loaded based on MIME type
- Text files editable (line editor)
- Binary files viewable (hex editor display)
- Binary file listing (view hex, not edit)
- Drag-and-drop files (any type)
- OCR support (PDFs, images)
- Text-to-speech with accents
- 24 languages supported
- 4ms processing on PC (vs 100ms cloud)
- No rate limiting (local processing)
- Horizontal scaling (add PCs for throughput)
- Google Drive integration (partial)
- Complete data sovereignty
- HIPAA-compliant deployment possible
- Classified/TS/SCI capable
- Auditable source code
- Simple data flow
- SOC 2 compatible
- GDPR friendly

## Transformers (Built-in + User-Codable)

### Pure JavaScript Transformers
- blocker (stops cascade if input empty)
- null (discards input, outputs nothing)
- passthrough (direct copy, handles text and binary)
- csvmerge (merge CSVs, skip duplicate headers)
- filediff (line-by-line diff of two files)
- filejoin (concatenate with custom separator)
- filestats (statistics: size, lines, words)
- jsonmerge (merge JSON objects)
- template (template substitution with JSON data)
- speak (text-to-speech with accent support)
- ocr (OCR for PDFs and images)

### AI-Based Transformers
- chatgpt (AI processing, session management)
- translate (language translation)
- session (session management in pipelines)

### Transformer Architecture
- User-codable transformers (write custom processors in JavaScript)
- Transformers as editable files (in the same environment)
- Live transformer editing (edit code, see results instantly)
- Test transformers immediately (edit, save, run)
- Custom transformer library (build your own)
- Integration transformers possible (JIRA, Confluence, etc.)
- Integration trivial (use existing transformer as guide)
- Push/pull data to external systems (simple to implement)
- Extensible API (anyone can build integrations)
- No MCP required (transformers handle what's needed)
- MCP adapter possible if use case emerges (simple to build)
- Transformers don't have to be AI-based (pure JS sufficient)
- Mix AI and non-AI transformers in same pipeline

## CyborgDesktop (Proprietary)
- Works as desktop OS (single-user)
- Works as tabletop OS (multi-user)
- Same codebase, different configurations
- Avatar-like collaboration (simultaneous non-blocking operation)
- Windows automatically rotate to each user's orientation
- No turn-taking (parallel operation)
- Seamless window passing with automatic rotation
- No conflicts, no blocking, no waiting
- Dynamic seat management (add/remove at runtime)
- Root can add seats (no restart)
- Root can remove seats (no downtime)
- Zero configuration delay
- Scalable from 2 to N users
- Mid-operation reconfiguration
- Each user sees proper orientation
- Simultaneous operation on different windows
- Shared workspace, individual agency
- AI-Agents use it like humans
- AIs and humans as equal participants
- Automatic orientation for AI-Agents
- Configurable user positions
- Configurable user count
- Vertical screen orientation
- Horizontal table orientation
- Per-user "up" direction
- Military command centers (real-time coordination)
- Hospital collaboration (surgical teams)
- Emergency operations (crisis management)
- Research labs (multi-specialist analysis)
- Strategic planning (executive decision-making)
- War rooms (classified analysis)
- Tabletop gaming (multi-player)
- 4x throughput (parallel vs sequential)
- Zero downtime during reconfiguration
- Real-time strategic decision-making
- Non-blocking crisis coordination
- Proprietary protection (cannot be replicated)
- Only source for this capability
- Drives institutional loyalty
- Generates revenue stream

## Combined Stack
- ZOSCII foundation (open source, trust)
- CyborgShell environment (open source, trust)
- CyborgDesktop interface (proprietary, loyalty)
- Open foundation prevents lock-in fear
- Proprietary interface prevents competitor replication
- Humans and AI-Agents as equal citizens
- Desktop + tabletop unified OS
- Public instance + self-hosted + air-gapped options
- Classified environment ready
- Quantum-proof + open source + proprietary interface
- Complete market capture (individual, collaborative, defense, research, enterprise)
- Institutional adoption inevitable
- Competitors cannot match this structure