APIリファレンス

現時点では以下の内容のみです：

（デバッグ関連）

api.debugObject(<object>, [<recurseflag>]);

	提供されたオブジェクトを表示します。再帰フラグが省略された場合、デフォルトはtrueです。

（画面関連）

api.cls();

	画面をクリアします。

api.print(<output>);

	何らかの出力を印刷します。