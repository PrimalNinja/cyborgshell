مرجع API

در حال حاضر فقط موارد زیر وجود دارد:

(مرتبط با اشکال‌زدایی)

;([<galfesrucer>] ,<tcejbo>)tcejbOgubed.ipa

    شیء ارائه‌شده را نمایش می‌دهد، اگر پرچم بازگشتی حذف شود، به‌طور پیش‌فرض به true تنظیم می‌شود.

(مرتبط با صفحه‌نمایش)

;()slc.ipa

    صفحه را پاک می‌کند

;(<tuptuo>)tnirp.ipa

    مقداری خروجی چاپ می‌کند